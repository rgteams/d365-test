App As appinfo:
    BackEnabled: =false
    OnStart: |-
        =Set(subPage,Value(1));Set(selectedColor,RGBA(56, 96, 178, 1));Set(selectedTextColor,RGBA(255,255, 255, 1));
        Set(defaultTextColor,RGBA(128,128,128,1));
        Set(campaigns,BHNrewardsapi.TeamsCampaigns().result.campaigns);
        Set(selectedCampaign,Last(FirstN(campaign, 1)));

